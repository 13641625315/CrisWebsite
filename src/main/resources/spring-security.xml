<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">


	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/css/**" access="permitAll" />
		<intercept-url pattern="/js/**" access="permitAll" />
		<intercept-url pattern="/image/**" access="permitAll" />
		<intercept-url pattern="/gif/**" access="permitAll" />
		<intercept-url pattern="/addon/**" access="permitAll" />
		<intercept-url pattern="/bootstrap/**" access="permitAll" />
		<intercept-url pattern="/indexPage" access="permitAll" />
		<intercept-url pattern="/loginPage" access="permitAll" />
		<intercept-url pattern="/util/captcha" access="permitAll" />
		<intercept-url pattern="/test/testPage" access="permitAll" />
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
		<form-login login-page="/loginPage" username-parameter="phoneNum"
			password-parameter="pass" default-target-url="/indexPage"
			authentication-failure-url="/loginPage?authenticated=false" />
		<logout invalidate-session="
			true " logout-success-url="/indexPage" />
		<session-management
			session-authentication-error-url="/loginPage">
			<!-- max-sessions只容许一个账号登录，error-if-maximum-exceeded 后面账号登录后踢出前一个账号，expired-url 
				session过期跳转界面 -->
			<concurrency-control max-sessions="1"
				error-if-maximum-exceeded="false" expired-url="/loginPage"
				session-registry-ref="sessionRegistry" />
		</session-management>
	</http>

	<authentication-manager>
		<authentication-provider user-service-ref="crisUserDetailsService">
			<password-encoder hash="md5">
				<!-- 将每个用户的username作为盐值 -->
				<salt-source user-property="username" />
			</password-encoder>
		</authentication-provider>
	</authentication-manager>

	<beans:bean id="sessionRegistry"
		class="org.springframework.security.core.session.SessionRegistryImpl">
	</beans:bean>

	<beans:bean id="md5PasswordEncoder"
		class="org.springframework.security.authentication.encoding.Md5PasswordEncoder">
	</beans:bean>

	<beans:bean id="crisUserDetailsService"
		class="com.cris.website.security.CrisUserDetailsService">
		<beans:property name="userServiceImpl" ref="userServiceImpl" />
	</beans:bean>
</beans:beans>